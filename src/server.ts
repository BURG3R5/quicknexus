export default function getServer(
  port: number,
  address: string,
  secure: boolean,
  allowDelete: boolean,
  lowerPortLimit: number,
  upperPortLimit: number,
  domain: string | undefined,
  maxSockets: number,
) {}
